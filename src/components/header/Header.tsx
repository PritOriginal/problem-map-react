import "./Header.scss"

export default function Header() {
    return (
        <header>
            <div className="header-container">
                <p>Карта проблем</p>
            </div>
        </header>
    );
}